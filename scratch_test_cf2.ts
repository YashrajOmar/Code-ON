import * as cheerio from 'cheerio'; async function test() { const res = await fetch('https://codeforces.com/problemset/problem/4/A'); const html = await res.text(); const c = cheerio.load(html); let tutorialUrl = null; c('.sidebar a').each((i, el) => { if (c(el).text().toLowerCase().includes('tutorial')) { tutorialUrl = c(el).attr('href'); } }); console.log('Tutorial:', tutorialUrl); } test();
